{
  "name": "pswp-graphic-tool",
  "private": true,
  "description": "Collaborative graphic editor. Frontend (React + Vite) and backend (Spring Boot) are siblings; see README.md.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:api": "cd backend && mvn spring-boot:run",
    "dev:all": "concurrently -n web,api -c cyan,magenta \"npm --prefix frontend run dev\" \"cd backend && mvn spring-boot:run\"",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run test",
    "test:api": "cd backend && mvn -q test",
    "test:all": "npm run test && npm run test:api",
    "lint": "npm --prefix frontend run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "version": "0.1.0"
}
